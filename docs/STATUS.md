---
doc_id: project-status
---

# Current status

DO NOT EDIT — generated from `PROJECT_STATE.yaml` and the active packet.

| Field | Value |
|---|---|
| Product | MustBeViral Studio |
| Engine | ViralGraph |
| Generation | `viralgraph-cleanroom-v2` |
| Launch customer | `dtc_ecommerce_marketing_teams` |
| Phase | P0 — Real Meta Campaign Launch Pack vertical slice and private MCP proof (in_progress) |
| Active packet | `WP-P0-001` |
| Current step | `p0-005-golden-launch-pack-runs` |
| Release target | `P0` |
| Pending decisions | None |
| Blockers | The representative-run-completion-and-latency gate is closed at 16 of 20 after the second authorized remediation completed GB-16 and GB-17. Median first-reviewable latency is 101.631 seconds and p90 is 122.314 seconds. Each run contained 16 unique succeeded provider jobs, complete private artifact/approval/export/receipt proof, an exact 4,550,000-micro capture, and zero residual. Both primaries succeeded, so GB-18 contingency was not run. Privileged PostgREST reported 10,650,000 micros current-day exposure, 89,350,000 remaining, and zero unsettled reservations after the runs. Golden-20 gross money is 86,450,000 quoted/reserved, 79,350,000 captured, 7,100,000 released, and zero residual. Remaining P0 blockers are production-segment Web Vitals, fully landed usable-pack cost, qualified evaluator outcomes, a paid pilot, and the operator decision. Evidence is governance/evidence/WP-P0-001/golden-20-run-proof.md section 11 and the GB-16, GB-17, and summary JSON records.; T4 replaced every golden-screen fixture port with the authenticated typed Worker client in money-safety order while preserving the locked visuals and explicit maximum-price confirmation. Browser traffic uses a fixed same-origin Core proxy because the Worker does not expose browser CORS; server traffic remains direct. A disposable staging identity proved sign-in and the real canvas read path for workspace 50f0b0c4-9302-4aab-8a03-d1953e71d8f7, project 314c25b4-a046-5d91-b715-f9bc2eebe24c, canvas 6690335b-517f-4590-a2ba-c2033014da81 and revision 97fd65c8-8765-4488-8400-2a06a8534e1a without submitting a run or spending provider money. The review rejection control remains a local draft because P0 has no rejection operation, and an export records the immutable export descriptor because P0 has no artifact-bytes endpoint. Evidence is governance/evidence/WP-P0-001/real-web-client-wiring.md.; The $0.67 rung is closed on staging. The single T2 run 8e724e99-1921-4096-ada8-71f93fe3a54f completed all 16 provider nodes through three waves and six dispatch epochs, captured exactly 4,550,000 reserved micros with zero release or residual, produced its first reviewable static in 128.375 seconds, approved all 16 outputs with an idempotent 16-artifact replay, returned a byte-identical ZIP on re-export, and exposed the immutable customer receipt with complete provider, model, cost and lineage reconciliation. Evidence is governance/evidence/WP-P0-001/full-pack-proof.md.; All six original execution-engine defects are now closed and proven live. The last one - nothing ever produced approved_output, so create_export could never succeed - closed with the $0.075 approval-and-export proof. One master plus one adaptation ran through real waves (the first live wave promotion), the adaptation was built from run-level brief context with a minted capability URL, POST /runs/:id/approvals promoted both artifacts in place (HTTP 200, idempotent replay), and POST /runs/:id/exports returned HTTP 201 with a byte-identical ZIP on re-export - the first successful export in the system's history. Captured exactly 700,000 micros, zero residual. Evidence in governance/evidence/WP-P0-001/approval-export-proof.md.; The TRUE root cause of every stranded settlement is found, fixed, and proven on the real path. Supabase loads pg_safeupdate on API connections only, and advance_run_readiness cleared its scratch table with a bare DELETE - so every Worker-side terminal advance failed with SQLSTATE 21000 while every direct-SQL validation passed. This, not the five missing grants, is what stranded both the copy incident and the fal master; the run in the proof above completed the rest of its lifecycle fully autonomously after the one-line fix (20260731020000_p0_readiness_safeupdate_fix), the first multi-wave settlement ever to do so without manual SQL. Standing verification doctrine - anything the privileged Worker calls must be verified through the PostgREST path (REST /rpc, apikey, service_role); pgTAP as postgres, direct SQL, and function fingerprints all passed while production was broken.; The $0.0004 copy-only money-path proof passed on live staging. Quoted 450,000 micros, captured exactly 450,000, zero residual, three JSON artifacts, run terminal succeeded, read back through the real GET /v1/runs/:id/receipt a customer would call. Getting there found and fixed FIVE missing service_role grants on privileged RPCs - three from today's Track C/D work (DROP FUNCTION clears grants; the migrations re-issued the revoke but not the matching grant) and two pre-existing from Track A (reap_dead_dispatch, finalize_cancel_requested_runs - both likely unreachable by the actual cron since they were introduced, because their own evidence validated them by calling the RPC directly as postgres, which bypasses grants). A new pgTAP suite (00021_p0_privileged_rpc_grants) asserts service_role execute on the full known privileged RPC surface so this class of defect fails loudly instead of shipping silently. A copy attempt whose synchronous settlement fails after submission has no automatic retry path once it leaves 'created' - folded into Track E's reconciler work rather than left open. Evidence in governance/evidence/WP-P0-001/copy-only-money-path-proof.md.; Five of the six original execution-engine defects are closed and each is proven on live staging. Signed artifact-access capabilities give the 10 image-to-image and image-to-video nodes a fetchable private-R2 input, proven by fal itself fetching one and returning a real edit. create_quote now derives dispatch waves from the pinned graph's edges and marks only wave 1 ready, while start_run_barrier creates every attempt eagerly and the gate moved to dispatch. Adaptation and motion nodes receive the run's brief and brand context plus exactly one upstream image. The OpenRouter copy route has a terminal path, an artifact and a capture. And the NaN capture that 503-looped every later fal webhook is gone. Evidence in governance/evidence/WP-P0-001/signed-artifact-access.md and staging-sync-tracks-c-and-d.md.; A wave-1 deadlock was found by adversarial review before it could cost anything, and is fixed. app_private.advance_run_readiness was reachable from exactly one place - inside advance_fal_provider_attempt, behind a provider_key='fal' join - while wave 1 of the launch pack is all three copy nodes on OpenRouter. The whole DAG would have stalled at wave 1 after copy was paid for. The shared settlement tail now lives in app_private.settle_attempt_transition and both provider entry points call it. Two further fal-only gates were shaped like validation and would each have looked already-fixed - a unit in ('image','video_second') filter in the ingest context lookup, and the same restriction again in TypeScript.; Staging is synced for Tracks C and D and the transfer was proven byte-exact - all nine changed functions match md5(pg_get_functiondef) against a local Postgres 17 with the migration files applied - but the staging migration HISTORY table still diverges from the repo's filenames. It carries 18 auto-named rows from earlier MCP applies with no local counterpart, which is why supabase db push refuses. Reconciling it means rewriting a record of what actually happened, so it is left as an explicit operator decision rather than quietly repaired.; Provider route identity and queue polling were both wrong and only live probing found either. The pinned Kontext application fal-ai/flux-kontext/pro does not exist on fal (its result reads back 404 "Path /pro not found"), which would have failed all 9 adaptation nodes after the 3 masters they depend on were paid for; and the driver constructed queue status/result URLs by appending to the full submit endpoint, which 405s for every route except flux-2-pro, so the reconciler could never observe a paid Kontext or Seedance artifact. Both are fixed and pinned by tests; see governance/evidence/WP-P0-001/signed-artifact-access.md section 4. The remaining lesson is that fal's queue returns 200 IN_QUEUE for any submit body and validates only at execution, so no submit-time success proves a route is callable.; The last known settlement-transition hole is closed. Forward migration 20260811000000_p0_poll_reconciliation_run_exit makes a poll-provider run leave reconciliation_required only after every attempt is terminal, with the established succeeded/partial_succeeded/failed/canceled aggregation and a WHERE guard that leaves unresolved runs parked; pgTAP suite 00024 covers both sides. refund_run_capture intentionally remains an operator-SQL-only P0 recovery tool until the human refund-authorization policy is accepted; it has no product or unattended Worker caller by design. Confirmation-token verification already closed in Track A with server-minted HMAC and constant-time verification.; T3 closed the response, authentication, and transport prerequisites for the web product. All 18 authenticated P0 REST operations now have operation-specific Zod success envelopes and a shared typed error envelope; compile-time and runtime parity checks pin the hand-written handler unions; generated OpenAPI covers health plus the complete 19-route Worker surface. The production Supabase SSR sign-in surface handles redirect-back, password errors, verification, rate limiting, expired-link notices, and local-session sign-out. The shared fetch client adds bearer JWT, Idempotency-Key on mutations, and X-Request-Id, then parses every response. Those prerequisites are now consumed by the seven T4 screen ports described above. Evidence is governance/evidence/WP-P0-001/response-contracts-sign-in-and-client.md.; P0 exit is gated on people rather than code. It needs 5-8 paid evaluator sessions (all eight slots still PENDING) and one signed paid pilot. Recruitment runs in parallel; sessions must not be held against fixtures.; The P1a charging model must enforce the fully-landed margin guardrail (at most $1.82 per pack for 60% margin), not the looser $5 gate. |
| Remote destructive action | `forbidden` |

## One next action

Apply record_provider_job_error_code on staging Postgres (lqvigvzqumpwfjikcvws only), then deploy the paired Worker and web with supplement-safe master-2 prompts. Do not reuse run 9b6e0619 or start another pack until that pair is live.
