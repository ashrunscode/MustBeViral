# W2.1 completion verification and independent review

Packet: `WP-PLATFORM-W2-001`. Step: `w2a-004-journeys-and-successor`. Evidence session: 2026-09-14.

Bounded website and text/Markdown/HTML document capture, immutable provenance and correctable unapproved knowledge drafts are verified for WashBodega and UnPile. The governed finish transition closes this packet; an offline repository pass alone does not close it.

## Final checks

| Check                                             | Result                                                                                                                                                |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime/import                                    | Node 24.18.0, pnpm 11.12.0, frozen install; verified public base and private continuation                                                             |
| W2 connected desktop Chromium and mobile Chromium | 8 passed, no skipped/flaky cases; saved website/document captures, corrections, error paths, duplicates, tenant/revocation behavior and accessibility |
| W1 connected regression                           | 14 passed, no skipped/flaky cases; onboarding, navigation, invitations without mail and truthful billing reads                                        |
| Full pgTAP                                        | 722 assertions in 45 files passed                                                                                                                     |
| Connected knowledge database                      | 6 checks passed through independent real database connections                                                                                         |
| Public-only egress probe                          | All three loopback forms blocked, zero private-target hits                                                                                            |
| pnpm agent:verify                                 | Passed, including every named database/governance/design/repository gate                                                                              |
| Independent review                                | Separate Grok 4.6 HIGH CLI session approved source, additive DB repairs and final acceptance evidence                                                 |

The verification receipt records exact run labels, runtime, timestamps, case results, source SHA-256 and normalized Git blobs, raw-log hashes, accessibility details and review identity. Raw environment-bearing logs, model streams and browser reports remain outside Git. No auth storage state, trace or video was recorded. Source implementation and reviews used Grok 4.6 with high reasoning through the local CLI; Codex coordinated gates and local process ownership.

## Acceptance mapping

### Safe source capture

Both laundry brands persist synthetic website and document captures through actual web, Core authorization, RPC, Postgres and private local R2. The only substituted boundary is outbound source HTTP for explicitly named synthetic hosts. HTTPS destination checks, connect-time public-only egress, redirect denial, 2 MiB content bounds, supported MIME types, fatal malformed-text handling and a total capture deadline remain enforced. Inline CLI/MCP document text is bounded to 32 KiB; larger raw bytes use the documented REST/web PUT companion.

Negative browser cases prove private URL and redirect denial, malformed and oversized content, a real stalled-body timeout, duplicate capture and forged identity recovery. The stalled source now uses bounded scheduled I/O and cancellation cleanup in the shared synthetic worker; the offline probe does not replace the browser's actual deadline proof. Fetched instructions and embedded JSON remain untrusted evidence; they do not alter permissions, approve knowledge, import campaign media or trigger execution.

Durable jobs retain bounded attempts. Both privileged completion and failure RPCs require the attempt generation captured at lease acquisition, independent of caller request IDs. SQL regressions prove an expired attempt cannot fail or complete its successor even when the correlation ID is reused. Exact workspace/brand/source storage keys and safe final URLs are enforced. Ambiguous database outcomes do not delete potentially committed private source objects.

### Provenance and correction

Every candidate retains its source identity, tenant/brand identity, capture time, excerpt and extraction method. Manual correction appends a revision and preserves ancestry. The no-website path reaches the same unapproved draft model; unknown gaps stay explicit. Both brands' captured sources survive reload and brand switching; saved manual corrections survive reload. Neither extraction nor correction creates an approved brand version.

### Tenant-safe reads

Actual grant and ownership checks cover viewers, outsiders, wrong parents, forged IDs, revoked actors, upload claims and immutable idempotent receipts. Connected SQL verifies per-brand privacy for identical source bytes, denied source fabrication and revoked reads. Browser delayed-response tests require a captured successful authorized response for the expected brand before switching or revoking, then release the exact body and await response completion plus render turns before checking the visible state. The owner's team page reads the accepted editor membership before invoking that editor's revoke action.

### No approval, outreach or spend

W1's real no-mail invitation and billing-read journeys remain green. No Stripe mutation, charging, provider execution, email, production mutation or public GitHub write occurred. The UI retains the accepted Lightfield studio direction and marks findings unapproved. Keyboard creation/correction/save and reload work at 200% CSS zoom on desktop/mobile; forced-colors/reduced-motion screenshots and axe/ARIA evidence are retained. CSS scaling is not a claim about browser-chrome zoom certification.

## Review and repair history

Independent review closed truthful error-envelope mapping, exact private storage keys, unsafe-URL error parity, binary PUT documentation, and genuine viewer/replay assertions. A separately reproduced retry-generation defect was fixed and independently approved. Full-suite verification then found nine missing FK covering indexes; a separate additive migration fixed the existing hardening invariant without weakening it or rewriting the already-applied migration. The connected DB harness now asserts expected SQL denials around whole transactions. Browser repairs addressed route-announcer selector collisions, explicit candidate selection, mobile scroll position, a real stalled fixture and fresh owner membership; all acceptance assertions remain present.

Packet specialist aliases unavailable on this workstation were not claimed as loaded. The build-mustbeviral workflow, available Windows/API/Supabase/Worker/React guidance and independent review supplied the relevant implementation and verification coverage. Existing accepted visual direction required no new owner decision.

## Environment, privacy and rollback

Canonical branch: `codex/viralgraph-cleanroom`. Public base: `966006beb9bc163019f6c04a564d820b577e1a6d`. Verified private continuation: `3ca1620c37796f611dc55ecad329a4843b998cd5`, tree `fbf8efd8686c063039485e09caa3dd124bbdc281`, with 43 private continuation commits at import. This packet began at `ea835215e3bd4c028950005a1b55d3323d403261`. The immutable packet/state policy, root dependency pins, workflows and instruction sources remain unchanged before the audited finish transition.

Only verified task-owned processes and MustBeViral's named Supabase stack used the coordinated local lane. Existing volumes and saved synthetic fixtures were retained through temporary handoffs. The final stop receipt confirms shutdown and the preserved volume. Local settings use the example's approved names; raw values are not evidence. Workstation-generated instruction adapters were temporarily formatted for repository verification and restored byte for byte; product checks were not skipped.

Rollback remains an application revert while retaining the additive source/draft schema and indexes. W1 contract regression is green against that additive schema. No tenant data was removed to simulate recovery.

## Successor

`successor-WP-PLATFORM-W2-002.yaml` is ready for W2.2–W2.4: representative extraction quality, labeled voice/audience/positioning proposals and owner-approved immutable brand versions. Every successor step and check remains pending. W2.5 lifecycle/change detection/catalog work is explicitly left for the proposed W2-003 continuation; it is not skipped. No provider, charging, outreach or production-write authority is added.
