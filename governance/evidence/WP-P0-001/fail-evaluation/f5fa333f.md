# Fail evaluation — GB-02 / f5fa333f

Work packet: WP-P0-001, step p0-005-golden-launch-pack-runs
Recorded 2026-08-18

No email, password, JWT, confirmation token, private object key, signed URL, raw provider payload,
prompt dump, or customer media is recorded.

## Machine record

- failedRunId: `f5fa333f-df35-4ce8-99f2-90ee9a78b7c7`
- kind: `content_policy_violation`
- cause: Image prompt named doctor, sleep, bedroom, muscle, medical, white coats, and body transformation as negatives; fal treated the mention as the subject.
- oneChange: `imageSafeText` on every image fragment, live on Worker `1c4b0b41`.
- changeDeployed: true
- bannedConceptStillInImagePrompt: false
- imageProbeSucceeded: false
- nextSpend: failed_image_probe
- retryDecision: stop
- helperReason: evaluation_stop

Superseded for retry by `governance/evidence/WP-P0-001/fail-evaluation/46d4e12b.md`. The
master-2 probe on Worker `1c4b0b41` still returned `content_policy_violation`. Do not retry the
same sanitizer.

`launchPackRetryDecision` for this record is `{ allowed: true, reason: 'evaluated_retry',
nextSpend: 'failed_image_probe' }`. Requesting `full_pack` is blocked until a succeeded
one-master probe. Same UTC day is not a field.

## What failed

- Failed nodes: master-2, master-3
- Stored provider_error_code: `fal_webhook_failed` (extractor later mapped `detail[].type`)
- Fal machine type / HTTP: `content_policy_violation` / 422
- Succeeded nodes retained: three copy sets, master-1, three adaptations of master-1, motion
- Captured / released / residual micros: 2,350,000 / 2,200,000 / 0

Sources: `governance/evidence/WP-P0-001/fal-webhook-failed-f5fa333f.md`,
`governance/evidence/WP-P0-001/gb02-image-prompt-policy-audit.md`.

## One cause

Named banned concepts in the FLUX.2 master prompt, not a dead route. master-1 on the same run
succeeded. Registered supplement `GB-12` completed on the same route during T5.

## One change being proven

Sanitized visual-only image prompts on Worker `1c4b0b41`. The next confirm must not add packshot
`image_url` or a catalog swap.

## Falsifier

If master-2 or master-3 return `content_policy_violation` again with a reconstructed prompt that
still contains no banned concept words, stop. Do not retry the same sanitizer.

## Cap check before confirm

- UTC day: 2026-08-18 (confirm still pending)
- Global remaining micros: check `get_global_spend_exposure` immediately before confirm
- Workspace-day remaining micros: check immediately before confirm
- Safe for one 4,550,000-micro quote: pending live cap read
