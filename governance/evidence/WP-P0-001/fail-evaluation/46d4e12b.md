# Fail evaluation — GB-02 master-2 probe / 46d4e12b

Work packet: WP-P0-001, step p0-005-golden-launch-pack-runs
Recorded 2026-08-18

No email, password, JWT, confirmation token, private object key, signed URL, raw provider payload,
prompt dump, or customer media is recorded.

## Machine record

- failedRunId: `46d4e12b-dc3b-4e41-8ac1-961057c700f1`
- kind: `content_policy_violation`
- cause: Sanitized visual-only master-2 prompt still returned fal `content_policy_violation`. The banned-word strip is not a sufficient change.
- oneChange: Replace SUPPLEMENT_MASTER_VISUAL_DIRECTIONS[1] with closed bottle, carton, and
  readable label only. Live on Worker 872ac183. Rollback target 1c4b0b41.
- changeDeployed: true
- bannedConceptStillInImagePrompt: false
- imageProbeSucceeded: false
- nextSpend: failed_image_probe
- retryDecision: stop
- helperReason: evaluation_stop

`launchPackRetryDecision` is `{ allowed: false, reason: 'evaluation_stop' }`. The named change ran as probe `17145ccd` and failed.

## What failed

- Failed nodes: master-2 (sole priced node)
- Stored provider_error_code: `content_policy_violation`
- Fal machine type / HTTP: `content_policy_violation`
- Succeeded nodes retained: none
- Captured / released / residual micros: 0 / 500,000 / 0

## One cause

The live `imageSafeText` master-2 prompt is still policy-rejected. This is not a full-pack
scheduling or money defect. It is not proved to be a leftover banned-word leak.

## One change being proven

`SUPPLEMENT_MASTER_VISUAL_DIRECTIONS[1]` is now `Material still life: closed bottle, carton, and readable label only.` Deployed on Worker `872ac183`. Packshot text, product name, and the “No …” tail are unchanged.

## Falsifier

A new master-2 probe that still returns `content_policy_violation` retires live-GB-02-16/16. Do not keep paying this brief.

## Cap check before confirm

- Probe quote: 500,000 micros
- Safe for one 500,000-micro probe: yes if `get_global_spend_exposure` remaining ≥ 500,000 (canary fail-closes otherwise)
- Safe for a 4,550,000-micro full pack: no. Full pack stays blocked until `imageProbeSucceeded` is true.

## Amendment 2026-08-18

Worker `872ac183` deployed. The authorized retry was probe `17145ccd`, which still returned
`content_policy_violation`. `retryDecision` is stop. Live-GB-02-16/16 is retired. Do not reuse
`46d4e12b` or `17145ccd`.
