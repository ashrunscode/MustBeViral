# Operator decisions of 2026-08-17: own the buyer product without rewriting the engine

Work packet: WP-P0-001, step p0-005-golden-launch-pack-runs. Recorded 2026-08-17.

The operator placed the agent in charge of making Studio buyer-honest. These decisions close
direction, not spend. They do not authorize a second architecture, V1 revival, invites, Stripe,
or a catalog swap onto Seedance 2.5.

## 1. P0 product shape

Decision: P0 is one owner, one campaign path, one $4.55 Meta Campaign Launch Pack, and Composed
Review. It is not a settings/users/projects studio and not an Ads Manager publisher.

Consequence: landing, dashboard, settings, and invites stay intentionally absent until a later
phase names them. Authority documents must say so instead of listing those screens as if they
already ship.

## 2. Keep the stack

Decision: Next.js/Vercel, Supabase Auth/Postgres/RLS, one Core Worker, private R2, fal-first
image/video, and the pinned OpenRouter copy route remain the platform. SuperDesign CLI stays
optional tooling. Lightfield tokens stay the visual system.

Consequence: Seedance 2.5 and any other new model stay research until official price, license,
retention, and a landed-cost canary exist. No second Worker, queue, or Durable Object in P0.

## 3. Last-mile obligations stay named, not invented tonight

Decision: buyer-ready last mile is ZIP download via existing `customer_download`, a working
packshot upload, offer-only copy wells, a named campaign header, and either a real reject
operation or a hidden Reject control. That work belongs in successor packet WP-P0-002 after the
queued GB-02 hardening run, not as a silent expand of p0-005.

Consequence: p0-005 remains one authorized pack after the 2026-08-18 UTC day reset. Do not start
another pack on 2026-08-17.

## 4. Status files carry open work only

Decision: `PROJECT_STATE.yaml` and the packet handoff list only current open blockers. Closed
proofs stay in evidence. `p0-006` is completed because MCP parity evidence already exists.
Live compute ids are Worker `37897d13` and web `jgifx4c95`.

Consequence: agents stop re-litigating settled settlement defects as if they were still on fire.
