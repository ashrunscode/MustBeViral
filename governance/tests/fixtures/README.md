# Governance fixtures

The executable negative cases live in `governance.test.mjs`. This directory is deliberately ignored by the cleanroom content scanner so synthetic secret and legacy canaries can be added without weakening production-path scanning.
